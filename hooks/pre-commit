#!/bin/sh
# Tracked in the repo so it survives a clone — see hooks/README.md.
# Bypass with `git commit --no-verify`. That bypass is a known hole (KB_DESIGN_PROPOSAL §7.3).
exec python3 "$(git rev-parse --show-toplevel)/scripts/check-vault.py" --staged
